CREATE TABLE product_feedback (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    product_id BIGINT,
    user_id BIGINT,
    rating INT,
    comment VARCHAR(1000),
    created_at DATETIME,
    source VARCHAR(255)
);